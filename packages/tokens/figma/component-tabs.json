{
  "Tabs": {
    "list-bg": {
      "$description": "Tabs: list bg.",
      "$value": "var(--color-bg-surface)",
      "$type": "color"
    },
    "list-border": {
      "$description": "Tabs: list border.",
      "$value": "var(--color-border-secondary)",
      "$type": "color"
    },
    "list-radius": {
      "$description": "Tabs: list radius.",
      "$value": "var(--radius-sm)",
      "$type": "number"
    },
    "trigger-bg": {
      "$description": "Tabs: trigger bg.",
      "$value": "var(--color-action-neutral-subtle)",
      "$type": "color"
    },
    "trigger-bg-hover": {
      "$description": "Tabs: trigger bg (hover state).",
      "$value": "var(--color-action-neutral-subtle-hover)",
      "$type": "color"
    },
    "trigger-bg-active": {
      "$description": "Tabs: trigger bg (active state).",
      "$value": "var(--color-bg-neutral-strong)",
      "$type": "color"
    },
    "trigger-text": {
      "$description": "Tabs: trigger text.",
      "$value": "var(--color-text-secondary)",
      "$type": "color"
    },
    "trigger-text-hover": {
      "$description": "Tabs: trigger text (hover state).",
      "$value": "var(--color-text-primary)",
      "$type": "color"
    },
    "trigger-text-active": {
      "$description": "Tabs: trigger text (active state).",
      "$value": "var(--color-text-inverse)",
      "$type": "color"
    },
    "trigger-border": {
      "$description": "Tabs: trigger border.",
      "$value": "var(--color-border-secondary)",
      "$type": "color"
    },
    "trigger-border-width": {
      "$description": "Tabs: trigger border width.",
      "$value": 1,
      "$type": "number"
    },
    "trigger-border-active": {
      "$description": "Tabs: trigger border (active state).",
      "$value": "var(--color-bg-neutral-strong)",
      "$type": "color"
    },
    "trigger-radius": {
      "$description": "Tabs: trigger radius.",
      "$value": "var(--radius-sm)",
      "$type": "number"
    },
    "trigger-height": {
      "$description": "Tabs: trigger height.",
      "$value": 38,
      "$type": "number"
    },
    "trigger-padding-x": {
      "$description": "Tabs: trigger padding x.",
      "$value": "var(--spacing-5)",
      "$type": "number"
    },
    "trigger-gap": {
      "$description": "Tabs: trigger gap.",
      "$value": "var(--spacing-2)",
      "$type": "number"
    },
    "trigger-font-size": {
      "$description": "Tabs: trigger font size.",
      "$value": "var(--text-sm)",
      "$type": "number"
    },
    "trigger-font-weight": {
      "$description": "Tabs: trigger font weight.",
      "$value": "var(--font-weight-medium)",
      "$type": "string"
    }
  }
}

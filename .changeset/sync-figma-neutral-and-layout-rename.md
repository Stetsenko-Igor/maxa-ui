---
"@maxa/tokens": patch
---

Sync confirmed manual Figma edits into code: `--color-neutral-200` (#E9EAEF → #EDEDED) and `--color-neutral-950` (#1B1A1A → #1A1919) primitive tweak, dark-mode `--color-bg-page` rebound from `--color-neutral-1000` to `--color-neutral-950` (a visibly lighter dark canvas), and the Figma `Layout` collection group rename `Stack`→`V-stack` / `Inline`→`H-stack` (naming only, values unchanged) reflected in `specs/foundations/spacing.md`.

# Visual QA Log: 2026-06-05

Scope: SocialButton update, component overview badge cleanup, PropsTable responsive behavior.

## Screenshots

- `/private/tmp/maxa-social-button-light-v2.png`
- `/private/tmp/maxa-social-button-dark-v2.png`
- `/private/tmp/maxa-social-button-mobile-v2.png`
- `/private/tmp/maxa-components-overview.png`
- `/private/tmp/maxa-table-light-v2.png`
- `/private/tmp/maxa-table-dark.png`
- `/private/tmp/maxa-table-mobile-v2.png`

## Result

- SocialButton renders provider icons instead of provider initials.
- SocialButton is readable in light and dark themes.
- SocialButton mobile layout does not overflow at 390px viewport.
- Components overview now shows `New` only for `Social Button` and `Utility Button`.
- PropsTable no longer clips long API values on mobile; rows collapse into stacked property cards.
- Table component docs render readable default, state, and cell-composition examples in light mode.
- Table mobile docs preserve access to wide table content through the table container while API reference remains responsive.

## Sweep pass — 2026-06-05 (stabilization bridge)

Automated Playwright sweep of **all 35 component docs pages × light + dark** (70 renders) for
console errors, page errors, missing `<h1>`, and non-200 status.

- **68 / 70 clean.** No JS/page errors, all pages return 200 with content in both themes.
- **1 real bug found + fixed:** Avatar docs "Default" live preview used `src="/avatar.png"`
  (no such asset → 404, silently fell back to initials). Switched the live render to the inline
  `AVATAR_IMAGE_SRC` data-URI; the displayed code sample still shows the idiomatic `/avatar.png`.
  Confirmed: avatar now renders the image, no 404.
- Targeted dark-mode screenshots reviewed: Select (open listbox), MultiSelect (chips + remove
  icons), DatePicker (single + range fields), Dialog, DropdownMenu, Table, Tabs — all render with
  correct contrast and layering. Select open-state listbox (selected/highlighted/disabled rows)
  verified correct in dark.

Screenshots: `/tmp/maxa-pw/shot-*.png` (avatar-light, select-dark-open, multiselect-dark-open,
datepicker-dark-open, dropdown-dark-open, dialog-dark-open, table-dark, tabs-dark).

### New-badge policy

Sidebar marks `Social Button`, `Table`, `Utility Button` as `New`. All three are the most recent
shipped tranche (2026-06-05), so all three correctly retain the badge. No change needed.

## Remaining (optional, lower priority)

- Interactive open-state captures for DropdownMenu / ContextMenu / DatePicker calendar were not
  reliably triggered by the sweep script (selector hit header controls). Open/close behavior is
  covered by unit + a11y tests; a manual pass with the browser extension can confirm pixels if
  desired.
- Per-page mobile (390px) capture of every page (PropsTable/code-block overflow already verified fixed).
